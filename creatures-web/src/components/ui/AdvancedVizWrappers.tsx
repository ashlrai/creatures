/**
 * Re-exports for advanced visualization components.
 * The components are now self-contained and read directly from the simulation store.
 */
export { CorrelationMatrix as CorrelationMatrixConnected } from './CorrelationMatrix';
export { PopulationProjection as PopulationProjectionConnected } from './PopulationProjection';
export { PowerSpectralDensity as PowerSpectralDensityConnected } from './PowerSpectralDensity';
